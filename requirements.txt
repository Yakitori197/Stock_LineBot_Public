# 皆設版本上限，避免日後重裝時解析到破壞相容性的新主版本

# LINE Bot SDK
line-bot-sdk>=3.14.0,<4

# Web Framework
flask>=3.0.0,<4

# 行情資料
# 統一由 yolab-quote 提供：台股代碼正規化（含 00631L 這類槓桿／反向 ETF）、
# yfinance 與 Yahoo 兩個資料源自動備援、批次併發查詢、中文名稱對照。
# [yfinance] extra 會帶入 yfinance 並鎖在 0.2 家族——1.x 是不相容的大改版。
# 原本的 requests / beautifulsoup4 / lxml 是給自建爬蟲用的，該段程式已移除。
yolab-quote[yfinance] @ git+https://github.com/Yakitori197/yolab-quote.git@main

# 環境變數
python-dotenv>=1.0.0,<2

# 排程（可選）
schedule>=1.2.0,<2
APScheduler>=3.10.0,<4

# 生產環境
gunicorn>=21.0.0,<27

# 開發
pytest>=7.4.0,<10
